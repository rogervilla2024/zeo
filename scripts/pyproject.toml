[project]
name = "seo-content-forge"
version = "0.1.0"
description = "Site-agnostic SEO helpers (sitemaps, JSON-LD, validation) for the seo-content-forge Claude Code toolkit."
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["orjson>=3.10", "pillow>=10"]

[project.scripts]
scf-sitemap = "generate_sitemap:main"
scf-jsonld = "build_jsonld:main"
scf-validate = "validate_schema:main"

[dependency-groups]
dev = ["pytest>=8.0", "mypy>=1.10", "ruff>=0.6"]

[tool.setuptools]
py-modules = [
    "build_jsonld", "generate_sitemap", "validate_schema",
    "generate_llmstxt", "generate_og_image", "generate_favicons",
    "generate_theme_css", "suggest_internal_links", "check_agent_ready",
    "check_pagespeed", "check_rich_results", "check_ai_patterns",
    "check_originality", "check_js_budget", "export_toolkit", "fetch_stock_images", "generate_logo", "check_broken_links", "mine_questions", "check_media_budget", "check_canonical_host", "seo_report", "fleet_report",
    "content_calendar", "queue_refresh_candidates", "check_toolkit_version",
    "check_affiliate_links", "check_article_images", "check_internal_links",
    "check_article_categories", "check_meta_quality", "check_launch_ready",
    "check_image_attrs", "check_robots_live", "check_freshness_live",
    "check_orphan_pages", "check_article_dates",
]
packages = ["seo_content_forge"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_unused_ignores = false

[tool.pytest.ini_options]
testpaths = ["tests"]
